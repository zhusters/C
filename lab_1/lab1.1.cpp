#include<iostream>
#include<math.h>

using namespace std;

void main()
{
	double x, y, z, a, b, c, rez;

	cin >> x >> y >> z;
	a = (1 + (pow(sin(x + y), 2)));
	b = fabs(x) - (y + y) / (1 + pow(x), 2 * pow(y), 2);
	c = pow(fabs(x), y) + (pow(cos(atan(1 / z)), 2)*);
	res = ((a / b) * (C));
	cout = << "ответ" << res;




}